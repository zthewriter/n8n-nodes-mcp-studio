# n8n-workflow pulls @n8n/expression-runtime, which compiles the native isolated-vm
# module. Nothing here needs it: this package only uses n8n-workflow's type
# definitions at build time, and n8n itself supplies the runtime. Skipping install
# scripts keeps `npm install` working on any Node version.
ignore-scripts=true
